= render @order
%div
  = link_to "Editar pedido", edit_order_path(@order)
  |
  = link_to "Seguir comprando", books_path
  - if Current.user.present? && Current.user.role == "client"
    |
    = link_to "Ver pedidos", shoping_path
  - if Current.user.present? && Current.user.role == "admin"
    |
    %br/
    %div
      = link_to "Registro de pedidos", orders_path
  
  =button_to "Eliminar pedido", @order, method: :delete
