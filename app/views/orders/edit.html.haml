%h1 Editar pedido
= render "form", order: @order
%br/
%div
  = link_to "Ver pedido", @order
  |
  = link_to "Seguir comprando", books_path
  - if Current.user.present? && Current.user.role == "admin"
    %br/
    %div
      = link_to "Registro de pedidos", orders_path
