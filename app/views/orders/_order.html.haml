%div{:id => "#{dom_id order}"}
  %p
    %strong User:
    = order.user_id
  %p
    %strong State:
    = order.state

  %p
    %strong Books: 
    - order.books.each do |book|
      = "- #{book.title}, #{book.price}€"
      %br/
  %p
    %strong Importe: 
    - @totalsum = order.books.map { |book| book.price }.sum
    = "#{@totalsum}€"
